name: Update Stock Data

on:
  schedule:
    - cron: "0 0 * * *"      # 매일 한국시간 오전 9시 (UTC 00:00)
    - cron: "0 21 * * 1-5"   # 평일 미국 장 마감 후 (UTC 21:00)
  workflow_dispatch:          # 수동 실행 가능

jobs:
  update-readme:
    runs-on: ubuntu-latest
    
    steps:
      - name: 리포지토리 체크아웃
        uses: actions/checkout@v3
      
      - name: Python 설치
        uses: actions/setup-python@v3
        with:
          python-version: "3.x"
      
      - name: Alpha Vantage API로 주식 데이터 업데이트
        run: |
          pip install requests
          python update_stock.py
        env:
          STOCK_API_KEY: ${{ secrets.STOCK_API_KEY }}
      
      - name: 변경 사항 커밋 및 푸시
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md stock_history.json
          git commit -m "🤖 자동 업데이트: 주식 데이터 갱신" || echo "No changes to commit"
          git push